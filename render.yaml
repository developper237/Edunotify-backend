# ══════════════════════════════════════════════════════════════════
# render.yaml — Blueprint Render pour EduNotify Backend
#
# Le billing-service a été créé manuellement sur Render Dashboard.
# Il se déploie via git push avec auto-deploy activé.
# Ce fichier ne contient PAS de config pour billing-service
# afin d'éviter les conflits de config (update_failed).
# ══════════════════════════════════════════════════════════════════

services: []
